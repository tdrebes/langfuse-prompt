identity:
  name: langfuse-prompt
  author: tdrebes
  label:
    en_US: langfuse-prompt
    zh_Hans: langfuse-prompt
    pt_BR: langfuse-prompt
description:
  human:
    en_US: Fetch prompts from langfuse prompt management
    zh_Hans: Fetch prompts from langfuse prompt management
    pt_BR: Fetch prompts from langfuse prompt management
  llm: Fetch prompts from langfuse prompt management
parameters:
  - name: query
    type: string
    required: true
    label:
      en_US: Query string
      zh_Hans: 查询语句
      pt_BR: Query string
    human_description:
      en_US: Fetch prompts from langfuse prompt management
      zh_Hans: Fetch prompts from langfuse prompt management
      pt_BR: Fetch prompts from langfuse prompt management
    llm_description: Fetch prompts from langfuse prompt management
    form: llm
extra:
  python:
    source: tools/langfuse-prompt.py
